[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "api👍"
version = "0.1.0"
description = "api de scraping"
authors = [
  { name="Seu Nome", email="seu@email.com" }
]
dependencies = [
  "flask",
  "requests",
  "beautifulsoup4"
]
